spring:
  application:
    name: zero-trust-rgw
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        namespace: public
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml  
        group: DEFAULT_GROUP
        namespace: public